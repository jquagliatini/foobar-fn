/**
 * This function is mainly used as a placeholder for an always true condition,
 * especially with the `cond` method
 *
 * ```
 * cond([[T, doSomething]])('foo');
 * ```
 *
 * @see cond
 */
export default function T(): true { // tslint:disable-line function-name
  return true;
}
